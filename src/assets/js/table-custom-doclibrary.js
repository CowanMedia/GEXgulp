var options = {
  valueNames: [ 'number', 'name', 'product',  ]
};

var doclist = new List('doclist', options);

var techlist = new List('techlist', options);

var proclist = new List('proclist', options);
